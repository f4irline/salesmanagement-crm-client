import React from 'react';

import './NavItem.css';

const NavItem = (props) => {
  return (
    <div className='NavItem'>
      <p className='item'>NavItem</p>
    </div>
  );
};

export default NavItem;